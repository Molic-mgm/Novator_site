// Runtime override for API endpoint.
// Измените apiUrl на полный адрес бэкенда, если фронтенд хостится отдельно.
// Например: "https://example.com" или "https://example.com/api".
// Пустое значение оставит запросы к /api на том же домене, что и фронтенд.
window.__APP_CONFIG__ = {
    apiUrl: "",
};

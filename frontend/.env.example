# Use an empty value to default API calls to the same origin the app is served from.
# For separate API hosting, set the full URL: e.g. https://api.yoursite.com/api
# (Нужно заполнить, если фронтенд хостится на статику без проксирования /api)
VITE_API_URL=https://api.example.com/api

# Optional media host. Defaults to VITE_API_URL without the `/api` suffix or the current origin.
VITE_MEDIA_URL=

# Optional site URL for structured data links; defaults to the domain serving the app.
VITE_SITE_URL=

# hCaptcha site key for the admin login form (optional but recommended in production).
VITE_HCAPTCHA_SITEKEY=1898a4f4-6000-49a4-87cc-a456c0dcf766
VITE_YMAPS_KEY=47af1c93-55ca-429b-b161-acf6ee1fb9de
